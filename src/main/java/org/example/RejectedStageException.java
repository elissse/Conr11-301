package org.example;

public class RejectedStageException extends Exception {
    RejectedStageException() {
        super("unfortunately this genius idea was rejected. but don't worry we will try again");
    }
}
